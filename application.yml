# 모든 어플리케이션에 들어갈 공통 설정
# 이 설정이 적용된 이후 모듈별로 설정이 적용된다
spring:
  datasource:
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
